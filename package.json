{
  "name": "order-service",
  "version": "1.0.0",
  "description": "microservice course -  order service",
  "main": "src/index.ts",
  "author": "AslamPangestu",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun build src/index.ts --target bun --outdir ./build",
    "start": "NODE_ENV=production bun build/index.js",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}